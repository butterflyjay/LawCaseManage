import { EntityManager } from "./entitymanager";
/**
 * sql执行器
 */
declare class SqlExecutor {
    /**
     * 执行mysql sql语句
     * @param connection    db connection
     * @param sql           待执行sql
     * @param params        参数数组
     * @param start         开始记录行
     * @param limit         最大记录行
     * @returns             执行结果或undefined
     */
    static exec(em: EntityManager, sql: string, params?: any[], start?: number, limit?: number): Promise<any>;
    /**
     * 执行mysql sql语句
     * @param connection    db connection
     * @param sql           待执行sql
     * @param params        参数数组
     * @param start         开始记录行
     * @param limit         最大记录行
     */
    private static execMysql;
    /**
     * 执行oracle sql语句
     * @param sql       待执行sql
     * @param params    参数数组
     */
    private static execOracle;
    /**
     * 执行mssql sql语句
     * @param sql       待执行sql
     * @param params    参数数组
     */
    private static execMssql;
    /**
     * 处理记录起始记录索引和记录数
     * @param sql       sql
     * @param start     开始索引
     * @param limit     记录数
     * @returns         处理后的sql
     * @since           0.2.0
     */
    private static handleStartAndLimit;
}
export { SqlExecutor };
