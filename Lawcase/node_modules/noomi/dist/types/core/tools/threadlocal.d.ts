/**
 * 线程安全（threadlocal）
 * @since 0.4.7
 */
declare class NoomiThreadLocal {
    /**
     * 线程id
     */
    private static threadId;
    /**
     * 异步线程存储器
     */
    private static localStorage;
    /**
     * 新建thread id
     * @returns     新threadId
     */
    static newThreadId(): number;
    /**
     * 获取当前thread id
     * @returns     当前threadId
     */
    static getThreadId(): number;
}
export { NoomiThreadLocal };
